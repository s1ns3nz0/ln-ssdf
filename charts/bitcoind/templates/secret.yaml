{{- if not .Values.auth.rpcPassword }}
{{- fail "auth.rpcPassword is required at install time" }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: bitcoind-rpc
type: Opaque
stringData:
  rpc-password: {{ .Values.auth.rpcPassword | quote }}
